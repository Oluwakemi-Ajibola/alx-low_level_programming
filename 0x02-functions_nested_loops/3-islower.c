#include "main.h"

/** _islower prints a function that checks for lowercase character
 * Return: i if letter is lowercase, 0 if letter is not lowercase
 */

int _islower(int c);
{
	if (c >= 'a'; && c <= 'z')
		return (i);
	else
		return (0);
}
