#include "main.h"

/**
 * _abs - computes the abs value of an integer
 * @n: the integer value
 * Return: the abs value of th einteger
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
